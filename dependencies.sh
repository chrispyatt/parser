# Run this file with 'bash dependencies.sh' prior to running the main script, if desired.

pip install argparse
pip install requests
pip install csv
pip install pandas
pip install json
pip install datetime
pip install dateutil
pip install matplotlib
pip install scipy
pip install numpy
pip install seaborn
pip install bokeh
